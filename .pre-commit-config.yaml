repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: ruff check .
        language: python
        additional_dependencies:
          - ruff>=0.15.12
        pass_filenames: false
        stages: [pre-commit, pre-push]
